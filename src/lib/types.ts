export interface IProject {
  image: string;
  title: string;
  description: string;
  slug: string;
}

export interface IProjectDetail {
  coverImage: string;
  productName: string;
  challenge: string;
  client: string;
  role: string;
  location: string;
  slug: string;
  images: string[];
}
